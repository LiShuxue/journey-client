// admin's
const login = `api/admin/login`
const register = `api/admin/register`
const test = `api/admin/test`

// visitor's

export default {
  admin: {
    login,
    register,
    test
  },
  visitor: {}
}
